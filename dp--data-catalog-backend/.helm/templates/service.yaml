apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.microservice }}
  labels:
    app: {{ .Values.microservice }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
  selector:
    app: {{ .Values.microservice }}
